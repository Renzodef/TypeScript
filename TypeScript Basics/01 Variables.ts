// Only in TypeScript (not in JavaScript) you can use annotations 
// and declare the type of the variables
let Name : string = 'Fred';
// You can declare the type
let age: number;
// And give it a value later
age = 21;
console.log(Name + " has " + age + " years."); // Fred has 21 years.